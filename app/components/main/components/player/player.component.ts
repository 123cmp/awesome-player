import { Component } from '@angular/core';

@Component({
    selector: 'player',
    moduleId: module.id,
    templateUrl: 'player.component.html',
})

export class PlayerComponent  {
    // name:String = 'Angular';
    // changeName() {
    //     this.name = 'q'
    // };
}